# Application Settings
APP_NAME=MaintainXtra
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# Database Configuration
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=maintenance_app
DB_USERNAME=root
DB_PASSWORD=

# SMTP2GO Configuration
MAIL_MAILER=smtp2go
SMTP2GO_API_KEY=your_api_key_here
MAIL_FROM_ADDRESS=noreply@yourdomain.com
MAIL_FROM_NAME="${APP_NAME}"

# SMTP2GO Webhook Settings
SMTP2GO_WEBHOOK_ENABLED=false
SMTP2GO_WEBHOOK_URL=
SMTP2GO_WEBHOOK_SECRET=

# SMTP2GO Rate Limiting
SMTP2GO_RATE_LIMIT_ENABLED=false
SMTP2GO_RATE_LIMIT_REQUESTS=100
SMTP2GO_RATE_LIMIT_PERIOD=60

# File Storage
FILESYSTEM_DISK=public

# Session & Cache
SESSION_DRIVER=file
CACHE_DRIVER=file

# Queue Configuration (for processing emails in background)
QUEUE_CONNECTION=database

# Security
SESSION_LIFETIME=120
SESSION_SECURE_COOKIE=false

# Maintenance App Specific Settings
MAINTENANCE_NOTIFICATION_EMAIL=notifications@yourdomain.com
MAINTENANCE_ADMIN_EMAIL=admin@yourdomain.com

# Optional: Third Party Services
# AWS S3 (if using S3 for file storage)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_URL=

# Optional: Payment Gateway (if implementing paid features)
STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=

# Optional: SMS Gateway (if implementing SMS notifications)
TWILIO_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# Optional: Google Maps API (if implementing location features)
GOOGLE_MAPS_API_KEY=

# Optional: Push Notifications (if implementing mobile push)
FIREBASE_CREDENTIALS=
FIREBASE_DATABASE_URL=

# Optional: Analytics
MIX_ANALYTICS_ID=

# Optional: Error Tracking
SENTRY_LARAVEL_DSN=
SENTRY_TRACES_SAMPLE_RATE=1.0

# Optional: Cache and Performance
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

# Optional: Logging
LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

# Optional: Maintenance Mode
MAINTENANCE_MODE=false
MAINTENANCE_MESSAGE="We're performing scheduled maintenance. We'll be back shortly!"

# Optional: Rate Limiting
RATE_LIMIT_REQUESTS=60
RATE_LIMIT_MINUTES=1

# Optional: API Settings (if implementing API)
API_RATE_LIMIT=60
API_RATE_LIMIT_MINUTES=1
API_TOKEN_EXPIRY=60

# Optional: Backup Settings
BACKUP_DISK=local
BACKUP_NOTIFICATION_EMAIL=backups@yourdomain.com
BACKUP_RETENTION_DAYS=7

# Optional: Image Processing
IMAGE_QUALITY=80
MAX_UPLOAD_SIZE=5120
ALLOWED_IMAGE_TYPES=jpg,jpeg,png,gif

# Optional: Security Headers
SECURITY_HEADERS_ENABLED=true
CONTENT_SECURITY_POLICY="default-src 'self'"
X_FRAME_OPTIONS=SAMEORIGIN
X_CONTENT_TYPE_OPTIONS=nosniff
REFERRER_POLICY=strict-origin-when-cross-origin

# Optional: Feature Flags
ENABLE_MAINTENANCE_REQUESTS=true
ENABLE_TECHNICIAN_ASSIGNMENT=true
ENABLE_EMAIL_NOTIFICATIONS=true
ENABLE_SMS_NOTIFICATIONS=false
ENABLE_PUSH_NOTIFICATIONS=false
ENABLE_FILE_UPLOADS=true
ENABLE_COMMENTS=true
ENABLE_RATINGS=true 